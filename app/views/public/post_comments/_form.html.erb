<%= form_with(model:[@post, @post_comment], local: false) do |f| %>
<%= f.text_area :comment, rows:'5',placeholder: "コメントを入力", class: "w-100" %>
<%= f.submit "コメントを投稿", class: "btn btn-sm btn-outline-warning mt-20 pull-right" %>
<% end %>