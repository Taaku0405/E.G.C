<% post.post_comments.each do |post_comment| %>
<%= image_tag post_comment.user.get_profile_image, size: "50x50" %>
<%= post_comment.user.name %><br>
<%= post_comment.created_at.strftime('%Y/%m/%d %H:%M:%S') %><br>
<%= post_comment.comment %><br>
<%= link_to "コメントを削除", admin_post_post_comment_path(post, post_comment), method: :delete, class: "btn btn-outline-danger pull-right", remote: true, data: {confirm: "本当に削除しますか？"}  %><br>
<% end %>